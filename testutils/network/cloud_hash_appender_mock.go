package network

// Code generated by http://github.com/gojuno/minimock (dev). DO NOT EDIT.

import (
	"context"
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock"
	"github.com/insolar/insolar/insolar"
)

// CloudHashAppenderMock implements storage.CloudHashAppender
type CloudHashAppenderMock struct {
	t minimock.Tester

	funcAppend          func(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte) (err error)
	inspectFuncAppend   func(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte)
	afterAppendCounter  uint64
	beforeAppendCounter uint64
	AppendMock          mCloudHashAppenderMockAppend
}

// NewCloudHashAppenderMock returns a mock for storage.CloudHashAppender
func NewCloudHashAppenderMock(t minimock.Tester) *CloudHashAppenderMock {
	m := &CloudHashAppenderMock{t: t}
	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.AppendMock = mCloudHashAppenderMockAppend{mock: m}
	m.AppendMock.callArgs = []*CloudHashAppenderMockAppendParams{}

	return m
}

type mCloudHashAppenderMockAppend struct {
	mock               *CloudHashAppenderMock
	defaultExpectation *CloudHashAppenderMockAppendExpectation
	expectations       []*CloudHashAppenderMockAppendExpectation

	callArgs []*CloudHashAppenderMockAppendParams
	mutex    sync.RWMutex
}

// CloudHashAppenderMockAppendExpectation specifies expectation struct of the CloudHashAppender.Append
type CloudHashAppenderMockAppendExpectation struct {
	mock    *CloudHashAppenderMock
	params  *CloudHashAppenderMockAppendParams
	results *CloudHashAppenderMockAppendResults
	Counter uint64
}

// CloudHashAppenderMockAppendParams contains parameters of the CloudHashAppender.Append
type CloudHashAppenderMockAppendParams struct {
	ctx       context.Context
	pulse     insolar.PulseNumber
	cloudHash []byte
}

// CloudHashAppenderMockAppendResults contains results of the CloudHashAppender.Append
type CloudHashAppenderMockAppendResults struct {
	err error
}

// Expect sets up expected params for CloudHashAppender.Append
func (mmAppend *mCloudHashAppenderMockAppend) Expect(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte) *mCloudHashAppenderMockAppend {
	if mmAppend.mock.funcAppend != nil {
		mmAppend.mock.t.Fatalf("CloudHashAppenderMock.Append mock is already set by Set")
	}

	if mmAppend.defaultExpectation == nil {
		mmAppend.defaultExpectation = &CloudHashAppenderMockAppendExpectation{}
	}

	mmAppend.defaultExpectation.params = &CloudHashAppenderMockAppendParams{ctx, pulse, cloudHash}
	for _, e := range mmAppend.expectations {
		if minimock.Equal(e.params, mmAppend.defaultExpectation.params) {
			mmAppend.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmAppend.defaultExpectation.params)
		}
	}

	return mmAppend
}

// Inspect accepts an inspector function that has same arguments as the CloudHashAppender.Append
func (mmAppend *mCloudHashAppenderMockAppend) Inspect(f func(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte)) *mCloudHashAppenderMockAppend {
	if mmAppend.mock.inspectFuncAppend != nil {
		mmAppend.mock.t.Fatalf("Inspect function is already set for CloudHashAppenderMock.Append")
	}

	mmAppend.mock.inspectFuncAppend = f

	return mmAppend
}

// Return sets up results that will be returned by CloudHashAppender.Append
func (mmAppend *mCloudHashAppenderMockAppend) Return(err error) *CloudHashAppenderMock {
	if mmAppend.mock.funcAppend != nil {
		mmAppend.mock.t.Fatalf("CloudHashAppenderMock.Append mock is already set by Set")
	}

	if mmAppend.defaultExpectation == nil {
		mmAppend.defaultExpectation = &CloudHashAppenderMockAppendExpectation{mock: mmAppend.mock}
	}
	mmAppend.defaultExpectation.results = &CloudHashAppenderMockAppendResults{err}
	return mmAppend.mock
}

//Set uses given function f to mock the CloudHashAppender.Append method
func (mmAppend *mCloudHashAppenderMockAppend) Set(f func(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte) (err error)) *CloudHashAppenderMock {
	if mmAppend.defaultExpectation != nil {
		mmAppend.mock.t.Fatalf("Default expectation is already set for the CloudHashAppender.Append method")
	}

	if len(mmAppend.expectations) > 0 {
		mmAppend.mock.t.Fatalf("Some expectations are already set for the CloudHashAppender.Append method")
	}

	mmAppend.mock.funcAppend = f
	return mmAppend.mock
}

// When sets expectation for the CloudHashAppender.Append which will trigger the result defined by the following
// Then helper
func (mmAppend *mCloudHashAppenderMockAppend) When(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte) *CloudHashAppenderMockAppendExpectation {
	if mmAppend.mock.funcAppend != nil {
		mmAppend.mock.t.Fatalf("CloudHashAppenderMock.Append mock is already set by Set")
	}

	expectation := &CloudHashAppenderMockAppendExpectation{
		mock:   mmAppend.mock,
		params: &CloudHashAppenderMockAppendParams{ctx, pulse, cloudHash},
	}
	mmAppend.expectations = append(mmAppend.expectations, expectation)
	return expectation
}

// Then sets up CloudHashAppender.Append return parameters for the expectation previously defined by the When method
func (e *CloudHashAppenderMockAppendExpectation) Then(err error) *CloudHashAppenderMock {
	e.results = &CloudHashAppenderMockAppendResults{err}
	return e.mock
}

// Append implements storage.CloudHashAppender
func (mmAppend *CloudHashAppenderMock) Append(ctx context.Context, pulse insolar.PulseNumber, cloudHash []byte) (err error) {
	mm_atomic.AddUint64(&mmAppend.beforeAppendCounter, 1)
	defer mm_atomic.AddUint64(&mmAppend.afterAppendCounter, 1)

	if mmAppend.inspectFuncAppend != nil {
		mmAppend.inspectFuncAppend(ctx, pulse, cloudHash)
	}

	params := &CloudHashAppenderMockAppendParams{ctx, pulse, cloudHash}

	// Record call args
	mmAppend.AppendMock.mutex.Lock()
	mmAppend.AppendMock.callArgs = append(mmAppend.AppendMock.callArgs, params)
	mmAppend.AppendMock.mutex.Unlock()

	for _, e := range mmAppend.AppendMock.expectations {
		if minimock.Equal(e.params, params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.err
		}
	}

	if mmAppend.AppendMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmAppend.AppendMock.defaultExpectation.Counter, 1)
		want := mmAppend.AppendMock.defaultExpectation.params
		got := CloudHashAppenderMockAppendParams{ctx, pulse, cloudHash}
		if want != nil && !minimock.Equal(*want, got) {
			mmAppend.t.Errorf("CloudHashAppenderMock.Append got unexpected parameters, want: %#v, got: %#v%s\n", *want, got, minimock.Diff(*want, got))
		}

		results := mmAppend.AppendMock.defaultExpectation.results
		if results == nil {
			mmAppend.t.Fatal("No results are set for the CloudHashAppenderMock.Append")
		}
		return (*results).err
	}
	if mmAppend.funcAppend != nil {
		return mmAppend.funcAppend(ctx, pulse, cloudHash)
	}
	mmAppend.t.Fatalf("Unexpected call to CloudHashAppenderMock.Append. %v %v %v", ctx, pulse, cloudHash)
	return
}

// AppendAfterCounter returns a count of finished CloudHashAppenderMock.Append invocations
func (mmAppend *CloudHashAppenderMock) AppendAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmAppend.afterAppendCounter)
}

// AppendBeforeCounter returns a count of CloudHashAppenderMock.Append invocations
func (mmAppend *CloudHashAppenderMock) AppendBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmAppend.beforeAppendCounter)
}

// Calls returns a list of arguments used in each call to CloudHashAppenderMock.Append.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmAppend *mCloudHashAppenderMockAppend) Calls() []*CloudHashAppenderMockAppendParams {
	mmAppend.mutex.RLock()

	argCopy := make([]*CloudHashAppenderMockAppendParams, len(mmAppend.callArgs))
	copy(argCopy, mmAppend.callArgs)

	mmAppend.mutex.RUnlock()

	return argCopy
}

// MinimockAppendDone returns true if the count of the Append invocations corresponds
// the number of defined expectations
func (m *CloudHashAppenderMock) MinimockAppendDone() bool {
	for _, e := range m.AppendMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.AppendMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterAppendCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcAppend != nil && mm_atomic.LoadUint64(&m.afterAppendCounter) < 1 {
		return false
	}
	return true
}

// MinimockAppendInspect logs each unmet expectation
func (m *CloudHashAppenderMock) MinimockAppendInspect() {
	for _, e := range m.AppendMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to CloudHashAppenderMock.Append with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.AppendMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterAppendCounter) < 1 {
		if m.AppendMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to CloudHashAppenderMock.Append")
		} else {
			m.t.Errorf("Expected call to CloudHashAppenderMock.Append with params: %#v", *m.AppendMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcAppend != nil && mm_atomic.LoadUint64(&m.afterAppendCounter) < 1 {
		m.t.Error("Expected call to CloudHashAppenderMock.Append")
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *CloudHashAppenderMock) MinimockFinish() {
	if !m.minimockDone() {
		m.MinimockAppendInspect()
		m.t.FailNow()
	}
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *CloudHashAppenderMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *CloudHashAppenderMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockAppendDone()
}
