metrics:
  zpagesenabled: false
ledger:
  lightchainlimit: 15
  is_postgres_base: true
  postgresql:
    url: postgres://postgres:s3cr3t@localhost/insolar?sslmode=disable&pool_max_conns=10
    migrationpath: ./migration
