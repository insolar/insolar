global:
  # evaluation_interval: 1m
  scrape_interval: 10s
  scrape_timeout: 2s

scrape_configs:
- job_name: insolard
## default = /metrics
# metrics_path:
  static_configs:
  - targets:
    - "localhost:8081"
    - "localhost:8082"
    - "localhost:8083"
    - "localhost:8084"
    - "localhost:8085"
