global:
  # evaluation_interval: 1m
  scrape_interval: 10s
  scrape_timeout: 2s

scrape_configs:
- job_name: insolard
## default = /metrics
# metrics_path:
  static_configs:
  - targets:
    - "localhost:8001"
    - "localhost:8002"
    - "localhost:8003"
    - "localhost:8004"
    - "localhost:8005"
