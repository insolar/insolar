syntax = "proto3";

package object;


import "github.com/gogo/protobuf/gogoproto/gogo.proto";

message IndexBucketRaw {
    option (gogoproto.equal) = false;

    bytes Lifeline = 1 [(gogoproto.nullable) = true];
    repeated bytes Requests = 2 [(gogoproto.customtype) = "github.com/insolar/insolar/insolar.ID", (gogoproto.nullable) = true];
    repeated bytes Results = 3 [(gogoproto.customtype) = "github.com/insolar/insolar/insolar.ID", (gogoproto.nullable) = true];
}
